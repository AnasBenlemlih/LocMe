import 'package:flutter/material.dart';

class AdminSocietesScreen extends StatelessWidget {
  const AdminSocietesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Gestion des Sociétés'),
      ),
      body: const Center(
        child: Text('Gestion des sociétés - À implémenter'),
      ),
    );
  }
}


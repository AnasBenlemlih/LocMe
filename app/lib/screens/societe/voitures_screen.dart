import 'package:flutter/material.dart';

class SocieteVoituresScreen extends StatelessWidget {
  const SocieteVoituresScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Mes Voitures'),
      ),
      body: const Center(
        child: Text('Gestion des voitures - À implémenter'),
      ),
    );
  }
}



import 'package:flutter/material.dart';

class SocieteReservationsScreen extends StatelessWidget {
  const SocieteReservationsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Réservations'),
      ),
      body: const Center(
        child: Text('Gestion des réservations - À implémenter'),
      ),
    );
  }
}



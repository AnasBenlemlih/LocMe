spring:
  application:
    name: locme-test
  
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: 
    driver-class-name: org.h2.Driver
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true
  
  security:
    jwt:
      secret: T807GhPnTJoAQ/VHSoIPQ3mb5ZperAGqaj8Rskr/kyo=
      expiration: 86400000

server:
  port: 8080

logging:
  level:
    com.locme: DEBUG